<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/ordersToolbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="8dp"
        android:paddingTop="48dp"
        android:paddingEnd="8dp"
        android:fitsSystemWindows="true"
        android:title="@string/order_history_title"
        android:titleTextColor="@color/primary_dark"
        app:navigationIconTint="@color/primary_dark"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/ic_back" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/ordersRecycler"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="24dp"
        app:layout_constraintTop_toBottomOf="@id/ordersToolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/ordersEmptyView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/orders_empty"
        android:textColor="@color/accent_gray"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/ordersRecycler"
        app:layout_constraintBottom_toBottomOf="@id/ordersRecycler"
        app:layout_constraintStart_toStartOf="@id/ordersRecycler"
        app:layout_constraintEnd_toEndOf="@id/ordersRecycler" />
</androidx.constraintlayout.widget.ConstraintLayout>

